config BYTEFS_FS
	tristate "BYTEFS: ByteFS"
	depends on FS_DAX
	depends on LIBNVDIMM
	select CRC32
	select LIBCRC32C
	help
	  Say Y here.

	  To compile this as a module, choose M here: the module will be
	  called bytefs.

	  If unsure, say N.
